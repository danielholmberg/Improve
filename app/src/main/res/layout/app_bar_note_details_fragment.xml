<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.appbar.AppBarLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/AppTheme.Base.FlatToolbar"
    android:background="@android:color/transparent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_note_details_fragment"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        app:layout_scrollFlags="scroll|enterAlways"
        app:popupTheme="@style/AppTheme.Base.FlatToolbar">

        <ImageButton
            android:id="@+id/close_dialog_btn"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_menu_close_primary"
            android:layout_gravity="start"
            android:layout_marginEnd="16dp"
            style="?android:attr/borderlessButtonStyle"
            android:layout_toStartOf="@id/toolbar_note_dialog_title_tv"
            android:layout_centerVertical="true" />

        <TextView
            android:id="@+id/toolbar_note_dialog_title_tv"
            style="@style/AppTheme.Base.ActivityTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:text="@string/note_activity_details" />

    </androidx.appcompat.widget.Toolbar>

</com.google.android.material.appbar.AppBarLayout>