<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/omm_cardview"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:clickable="true"
    android:elevation="@dimen/card_elevation"
    android:layout_margin="@dimen/card_item_margin">
    <LinearLayout
        android:id="@+id/omm_item_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/omm_item_marker_iv"
            android:layout_width="@dimen/omm_item_marker_width"
            android:layout_height="match_parent"
            android:background="@color/ommOrange"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/omm_item_body_padding"
            android:background="@color/cardInfoBackground">
            <android.support.constraint.ConstraintLayout
                android:id="@+id/omm_body_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:id="@+id/title_tv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:paddingBottom="@dimen/omm_title_padding"
                    android:maxLines="@integer/omm_item_title_max_lines"
                    android:ellipsize="end"
                    android:textStyle="bold"
                    android:textAppearance="@style/TextAppearance.AppCompat.Large"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <TextView
                    android:id="@+id/info_tv"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="@integer/omm_item_info_max_lines"
                    android:paddingBottom="@dimen/omm_item_body_padding"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/title_tv" />

            </android.support.constraint.ConstraintLayout>

            <include
                android:id="@+id/footer_omm"
                layout="@layout/omm_item_footer"/>
        </LinearLayout>

    </LinearLayout>
</android.support.v7.widget.CardView>