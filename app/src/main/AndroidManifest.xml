<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.danielholmberg.improve">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="stateAlwaysHidden"
        android:theme="@style/AppTheme.Base">
        <activity
            android:name="dev.danielholmberg.improve.Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Base.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="dev.danielholmberg.improve.Activities.AddOnMyMindActivity"
            android:label="@string/lable_add_onmymind"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName="dev.danielholmberg.improve.Activities.MainActivity"/>
        <activity
            android:name="dev.danielholmberg.improve.Activities.AddContactActivity"
            android:label="@string/lable_add_contact"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName="dev.danielholmberg.improve.Activities.MainActivity"/>
    </application>

</manifest>